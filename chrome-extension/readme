Once the site loads , and the competition starts ... you'll be prompted with an input pop-up , just add the delay in milliseconds between each character ....
note however that when you finish just press the 'Escape' key to terminate it .
i should add this feature to automatically stop after all the words gets entered but anyways
